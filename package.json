{
  "name": "ink-app",
  "version": "1.0.0",
  "description": "Ink — Handwriting Journal (native Android wrapper)",
  "private": true,
  "scripts": {
    "build": "echo 'Static site — nothing to build; www/ is the deliverable'",
    "sync": "npx cap sync android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.1",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
